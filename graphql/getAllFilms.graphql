query getAllFilms ($order: [Sort!], $fields: [String!]) {
  films(orderBy: {order: $order, fields: $fields}) {
    _id
    title
    year
    directors {
      name
      lexKey
      birthYear
      deathYear
    }
  }
}
